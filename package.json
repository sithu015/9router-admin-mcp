{
  "name": "9router-admin-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Initial MCP server for 9router admin API operations",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test test/**/*.test.ts",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2"
  }
}
